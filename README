Play application
=================================

This file will be packaged with your application when using `activator dist`.

To run "playApp" from the command line, "cd playApp" then:
activator run

To run the test for "playApp" from the command line, "cd playApp" then:
activator test

To run the Activator UI for "playApp" from the command line, "cd playApp" then:
activator ui